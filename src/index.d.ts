type line = {
    width        : number,
    height       : number,
    x            : number,
    y            : number,
    speed        : number,
    color        : string
};

type color = {
    r : number,
    g : number,
    b : number
};

type windowDimensions = {
    width  : number,
    height : number
}